{
  "name": "private-chat-web",
  "version": "1.0.0",
  "private": true,
  "description": "Static web admin and optional API for Private Chat messages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb": "^7.5.0"
  }
}
